// Which of the following statements is correct?
// PRIVATE, PACAKGED, PROTECTED, PUBLIC
// A package access is more strick then protected access. because package access only allow all classes in the same package. and the protected access allow access to a class on the same package and his subclasses.
// A. Package access is more lenient that 'protected' access. -- not true

// The class is vissible to all classes inside the package of outside the package because it has public access modifier. Only the fields and method is not accessable because they are private. but the class is public.
// B. A `public` class that has private fields and package methods is not visible to classes outside the package. --not true

// U can use Access modifier to restrick access outside or inside the same package. u can not restric a class to access only particular class indie the same package.
// C. You can use access modifiers so only some of the classes in a package see a particular package class. -- not true

// U can use access modifier to set all methode to be accesible and to set all instance variable to private so they not accesable outside own class. but u need to put access modifier one by one bon all methode and instance variable.
// D. You can use access modifiers to allow access to all methods and not any instance variables. -- true

// u cannot use access modifiers to restrict access to all classes that begin with the wordt Test. In java we dont have a GLOB function to make this functionality true.
// E. You can use access modifiers to restrict access to all classes that begin with the wordt Test. -- not true


---
Can u check my answere and review it? and if im wrong can u explain me better? this is about Java OCP 21 exam questions.
U are an expert in java en u can explain it very wel.